# Dependencies
node_modules/
venv/
__pycache__/
*.pyc

# Environment & secrets — NEVER commit these
.env
.env.local
.env.*.local
FrontMaxBreak/.env
FrontMaxBreak/.env.local
FrontMaxBreak/google-services.json
FrontMaxBreak/google-services-preview.json
FrontMaxBreak/google-play-service-account.json
maxBreak/.env

# Expo / EAS
FrontMaxBreak/.expo/
FrontMaxBreak/dist/

# Django
maxBreak/staticfiles/
maxBreak/media/
*.sqlite3

# ML model files (generated, not committed — run retrain_prediction_model to recreate)
maxBreak/ml_models/*.pkl
maxBreak/ml_models/*.json

# OS
.DS_Store
Thumbs.db
